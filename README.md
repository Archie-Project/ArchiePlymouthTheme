# Archie Plymouth Theme
A Plymouth theme based in spinner featuring Archie in pixel art style.

Clone this repo, rename the folder to `archie` and copy it to `/usr/share/plymouth/themes`,
then execute:
```bash
sudo plymouth-set-default-theme -R archie
```
